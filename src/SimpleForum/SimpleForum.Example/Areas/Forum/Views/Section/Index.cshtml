@model SimpleForum.Web.Models.Section.SectionIndexModel

<h2>@Model.Title</h2>

<p>
	<a href="#" class="btn btn-info">Post a new topic</a>
</p>

<table class="table">
	<thead>
		<tr>
			<th>Topics</th>
			<th>Replies</th>
			<th>Views</th>
			<th>Last post</th>
		</tr>
	</thead>
	<tbody>
		@foreach (var topic in Model.Topics)
		{
			<tr>
				<td>
					<a href="@Url.Action("Index", "Section", new { id = topic.Id })">@topic.Subject</a>
				</td>
				<td></td>
				<td></td>
				<td></td>
			</tr>
		}
	</tbody>
</table>
